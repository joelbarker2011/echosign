version: 2.1

executors:
  ruby:
    parameters:
      version:
        description: "ruby version"
        type: string
    docker:
      - image: cimg/ruby:<<parameters.version>>

jobs:
  ruby:
    parameters:
      version:
        description: "ruby version"
        type: string
    executor:
      name: ruby
      version: <<parameters.version>>
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dep-{{ .Branch }}-<< parameters.version >>-{{ checksum "Gemfile.lock" }}
            - v1-dep-master-<< parameters.version >>-{{ checksum "Gemfile.lock" }}
            - v1-dep-{{ .Branch }}-<< parameters.version >>
            - v1-dep-master-<< parameters.version >>
      - run:
          name: Install gems
          command: bundle config path vendor/bundle && bundle check || bundle install --jobs=4 --retry=3
      - save_cache:
          key: v1-dep-{{ .Branch }}-<< parameters.version >>-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle
      - run:
          name: Run RuboCop
          command: bundle exec rubocop --fail-fast
      - run:
          name: Run Rake
          command: bundle exec rake

      # Save artifacts
      - store_artifacts:
          path: coverage/

      # Save test results
      - store_test_results:
          path: test-results

workflows:
  test-matrix:
    jobs:
      - ruby:
          matrix:
            parameters:
              version:
                - "3.0"
                - "3.1"
                - "3.2"
                - "3.3"
                - "3.4"
  weekly:
    jobs:
      - ruby:
          matrix:
            parameters:
              version:
                - "3.0"
                - "3.1"
                - "3.2"
                - "3.3"
                - "3.4"
    triggers:
      - schedule:
          cron: "0 0 * * 0"
          filters:
            branches:
              only:
                - master
